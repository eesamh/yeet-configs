{
  "mcpServers": {
    "awslabs.redshift-mcp-server": {
      "command": "uvx",
      "args": ["awslabs.redshift-mcp-server@0.0.19"],
      "env": {
        "AWS_PROFILE": "${AWS_PROFILE}",
        "AWS_DEFAULT_REGION": "${AWS_REGION}",
        "FASTMCP_LOG_LEVEL": "INFO"
      }
    }
  }
}
